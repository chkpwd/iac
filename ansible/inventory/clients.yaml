---
all:
  hosts:
    localhost:
  children:
    vsphere:
      children:
        esxi_hosts:
        vcenter:

    vcenter:
      hosts:
        ronin:

    esxi_hosts:
      hosts:
        # esx[1:3]:
        octane:
        caustic:

    guests:
      children:
        docker_hosts:
        media_server:
        k3s:
      hosts:
        stable-diffusion:

    windows:
      hosts:
        bloodhound:
        revenant:
      vars:
        ansible_become_method: runas
        ansible_become_user: "{{ ansible_ssh_user }}"
        ansible_ssh_user: administrator
        ansible_shell_type: powershell
        ansible_connection: ssh

    docker_hosts:
      hosts:
        mirage:
        horizon:
        valkyrie:
        crypto:
        node-01:
    
    k3s:
      hosts:
        kubes-cp-1:
        kubes-cp-2:
        kubes-cp-3:

    printers:
      hosts:
        revenant:

    media_server:
      hosts:
        mirage:

    dev:
      children:
        staging:

    staging:
      hosts:
        jumpbox-dev: 
          ansible_host: 172.16.16.232

    cloud:
      hosts:
        149.28.65.221:

    onprem:
      children:
        vsphere:

    datacenter:
      children:
        cloud:
        onprem:
