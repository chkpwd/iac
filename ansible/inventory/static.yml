---
all:
  hosts:
    localhost:
      ansible_connection: local
  children:
    proxmox:
      hosts:
        pve-srv-01:
          ansible_user: root
    linux:
      hosts:
        mgmt-srv-01:
        ai-inference-01:
        pve-srv-01:
        gs-srv-01:
    windows:
      hosts:
        chk-gaming-01:
      vars:
        ansible_become_method: runas
    docker_hosts:
      hosts:
        mgmt-srv-01:
        gs-srv-01:
    dns_servers:
      hosts:
        gravity-dns-02:
        mgmt-srv-01:
    game_servers:
      hosts:
        gs-srv-01:
      vars:
        ansible_user: root
    printers:
      hosts:
        print-srv-01:
      vars:
        ansible_user: pi
        ansible_become_user: pi
    cloud:
      children:
        aws:
