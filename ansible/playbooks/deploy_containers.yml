---
- name: "Install docker containers"
  hosts: guests
  become: true
  module_defaults:
    community.docker.docker_container:
      restart_policy: always
  
  roles:
    - { role: ../roles/compose }