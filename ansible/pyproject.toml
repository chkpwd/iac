[tool.poetry]
package-mode = false

[tool.poetry.dependencies]
python = ">=3.10"
requests = "^2.32.3"
yamllint = "^1.37.0"
ansible-lint = "^25.2.1"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[tool.ruff.lint]
extend-select = ["I"]
