---
# Config DIR
pms_app_dir: /opt/docker-confs/plex

# Media DIR
media_path: /home/{{ common_user }}/media

# Backup directory
backup_path: /home/{{ common_user }}/media/backup/plex

discord_media_webhook: "{{ DISCORD_PLEX_BACKUP_WEBHOOK }}"

media_nfs_shares:
  - media
  - downloads
  - backups
