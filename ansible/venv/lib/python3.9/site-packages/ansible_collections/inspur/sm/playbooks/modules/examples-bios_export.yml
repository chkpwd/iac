- name: bios test
  hosts: ism
  connection: local
  gather_facts: no
  vars:
    ism:
      host: "{{ ansible_ssh_host }}"
      username: "{{ username }}"
      password: "{{ password }}"

  tasks:

  - name: "export bios config"
    inspur.sm.bios_export:
      file_url: "/home/wbs/bios.json"
      provider: "{{ ism }}"