- name: bmc info test
  hosts: ism
  connection: local
  gather_facts: no
  vars:
    ism:
      host: "{{ ansible_ssh_host }}"
      username: "{{ username }}"
      password: "{{ password }}"

  tasks:

  - name: "get BMC information"
    inspur.sm.bmc_info:
      provider: "{{ ism }}"