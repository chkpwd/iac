- name: bios test
  hosts: ism
  connection: local
  gather_facts: no
  vars:
    ism:
      host: "{{ ansible_ssh_host }}"
      username: "{{ username }}"
      password: "{{ password }}"

  tasks:

  - name: "set bios setup"
    inspur.sm.edit_bios:
      attribute: "VMX"
      value: "Disable"
      provider: "{{ ism }}"

  - name: "set bios setup"
    inspur.sm.edit_bios:
      attribute: "VMX"
      value: "Enable"
      provider: "{{ ism }}"