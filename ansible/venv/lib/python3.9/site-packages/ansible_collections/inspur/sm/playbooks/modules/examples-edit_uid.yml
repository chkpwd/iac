- name: UID test
  hosts: ism
  connection: local
  gather_facts: no
  vars:
    ism:
      host: "{{ ansible_ssh_host }}"
      username: "{{ username }}"
      password: "{{ password }}"

  tasks:

  - name: "Set uid"
    inspur.sm.edit_uid:
      led: "on"
      time: 10
      provider: "{{ ism }}"

  - name: "Set uid"
    inspur.sm.edit_uid:
      led: "off"
      provider: "{{ ism }}"