- name: Onboard test
  hosts: ism
  connection: local
  gather_facts: no
  vars:
    ism:
      host: "{{ ansible_ssh_host }}"
      username: "{{ username }}"
      password: "{{ password }}"

  tasks:

  - name: "Get onboard disks information"
    inspur.sm.onboard_disk_info:
      provider: "{{ ism }}"