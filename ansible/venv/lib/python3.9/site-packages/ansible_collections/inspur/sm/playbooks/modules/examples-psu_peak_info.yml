- name: psu peak test
  hosts: ism
  connection: local
  gather_facts: no
  vars:
    ism:
      host: "{{ ansible_ssh_host }}"
      username: "{{ username }}"
      password: "{{ password }}"

  tasks:

  - name: "get psu peak information"
    inspur.sm.psu_peak_info:
      provider: "{{ ism }}"