FROM debian:10
RUN apt-get update \
    && apt-get install -y \
         python sudo bash ca-certificates iproute2 python-apt aptitude \
    && rm -rf /var/lib/apt/lists/* \
    && rm -rf /usr/share/doc \
    && rm -rf /usr/share/man \
    && apt-get clean
