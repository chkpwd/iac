---
apiVersion: v1
kind: Service
metadata:
  name: meshmon
  namespace: tools
spec:
  ports:
    - name: http
      port: 8000
      protocol: TCP
      targetPort: 8000
  selector:
    app.kubernetes.io/name: meshmon
  type: ClusterIP
