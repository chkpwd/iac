---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/kustomize.toolkit.fluxcd.io/kustomization_v1.json
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: &app palworld
  namespace: &ns palworld
spec:
  targetNamespace: *ns
  path: ./kubernetes/apps/palworld/
  commonMetadata:
    labels:
      app.kubernetes.io/name: *app
  components:
    - ../../core/keda-operator/_components/nfs
  postBuild:
    substitute:
      APP: *app
  prune: true
  sourceRef:
    kind: GitRepository
    name: chkpwd-ops
    namespace: flux-system
  wait: false
  interval: 30m
