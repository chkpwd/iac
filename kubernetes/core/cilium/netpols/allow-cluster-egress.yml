---
# yaml-language-server: $schema=https://chkpwd.github.io/iac/cilium.io/ciliumclusterwidenetworkpolicy_v2.json
apiVersion: cilium.io/v2
kind: CiliumClusterwideNetworkPolicy
metadata:
  name: allow-cluster-egress
spec:
  endpointSelector:
    matchExpressions:
      - key: egress.home.arpa/custom-egress
        operator: DoesNotExist
  egress:
    - toEndpoints:
        - {}
    - toEntities:
        - cluster
    - toEntities:
        - world
