---
apiVersion: helm.toolkit.fluxcd.io/v2beta2
kind: HelmRelease
metadata:
  name: crunchy-postgres-operator
spec:
  interval: 30m
  chart:
    spec:
      chart: pgo
      version: 5.5.1
      interval: 30m
      sourceRef:
        kind: HelmRepository
        name: crunchydata
        namespace: flux-system

  install:
    crds: CreateReplace
  upgrade:
    crds: CreateReplace
