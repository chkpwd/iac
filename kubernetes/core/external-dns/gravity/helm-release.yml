---
# yaml-language-server: $schema=https://chkpwd.github.io/iac/helm.toolkit.fluxcd.io/helmrelease_v2.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: gravity-external-dns
spec:
  interval: 1h
  chart:
    spec:
      chart: external-dns
      interval: 30m
      version: 1.20.0
      sourceRef:
        kind: HelmRepository
        name: external-dns
  values:
    logLevel: debug
    podSecurityContext:
      fsGroup: 65534
    securityContext:
      runAsNonRoot: true
      runAsUser: 65534
      readOnlyRootFilesystem: true
      capabilities:
        drop: ["ALL"]
    provider:
      name: webhook
      webhook:
        image:
          repository: ghcr.io/beryju/gravity-external-dns
          tag: latest@sha256:96c004225e6c45240421d714b535718ecf3a3fa8c4f21f6364255c1f12ea5c88
        env:
          - name: GRAVITY_URL
            value: http://mgmt-srv-01.chkpwd.com:8008/
          - name: GRAVITY_TOKEN
            valueFrom:
              secretKeyRef:
                name: gravity-external-dns
                key: gravity-token
          - name: DOMAIN_FILTER
            value: "chkpwd.com."
        livenessProbe:
          httpGet:
            path: /healthz
            port: http-webhook
          initialDelaySeconds: 10
          timeoutSeconds: 5
        readinessProbe:
          httpGet:
            path: /healthz
            port: http-webhook
          initialDelaySeconds: 10
          timeoutSeconds: 5
        resources:
          requests:
            memory: 20Mi
            cpu: 10m
          limits:
            memory: 100Mi
    extraArgs:
      - --gateway-name=private
      # - "--txt-prefix=reg-%{record_type}-"
    sources: ["crd", "gateway-httproute"]
    policy: upsert-only
    registry: noop
    domainFilter: ["chkpwd.com"]
    serviceMonitor:
      enabled: false
    resources:
      requests:
        cpu: 10m
        memory: 100Mi
      limits:
        memory: 300Mi
