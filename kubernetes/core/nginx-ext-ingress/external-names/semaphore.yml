---
apiVersion: v1
kind: Service
metadata:
  name: semaphore-ui-internal
spec:
  type: ExternalName
  externalName: mgmt-srv-01.chkpwd.com
  ports:
    - name: http
      port: &port 3005
      protocol: TCP
      targetPort: *port
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: &app semaphore-ui-internal
  labels:
    external-dns/private: "true"
  annotations:
    external-dns.alpha.kubernetes.io/target: "chkpwd.com"
spec:
  ingressClassName: ext-ingress
  rules:
    - host: semaphore.chkpwd.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: *app
                port:
                  number: 3005
