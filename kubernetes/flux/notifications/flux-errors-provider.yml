---
# yaml-language-server: $schema=https://chkpwd.github.io/iac/notification.toolkit.fluxcd.io/provider_v1beta3.json
apiVersion: notification.toolkit.fluxcd.io/v1beta3
kind: Provider
metadata:
  name: &name flux-errors
  namespace: flux-system
spec:
  type: discord
  channel: *name
  secretRef:
    name: notif-errors
  username: k3s-errors-bot
