[tool.poetry]
package-mode = false

[tool.poetry.dependencies]
python = ">=3.10"
flux-local = ">=7.5"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[tool.ruff.lint]
extend-select = ["I"]
