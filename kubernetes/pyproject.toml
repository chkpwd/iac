[tool.poetry]
package-mode = false

[tool.poetry.dependencies]
python = ">=3.14,<3.15"
flux-local = ">=8.2"
yamlpath = "^3.9.1"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[tool.ruff.lint]
extend-select = ["I"]
