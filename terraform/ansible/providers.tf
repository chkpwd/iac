#===============================================================================
# Ansible Provider
#===============================================================================

terraform {
  required_providers {
    ansible = {
      source  = "ansible/ansible"
      version = "1.1.0"
    }
  }
}